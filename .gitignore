node_modules/
.env

# GCP service account keys — NEVER commit real ones
*.json.key
service-account*.json
!service-account.example.json

# JWT signing keys for Salesforce — never commit
server.key
server.crt

.DS_Store
